language: python
python:
  - "3.6.5"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
  - pip install -r requirements.txt
script:
  - pytest
after_success:
  - coveralls